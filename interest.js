const interest = {

    init(){
        const tagNode = document.querySelector("#interest")
        tagNode.addEventListener("keypress", (pressEvent) => {
            const inputCharacter = pressEvent.key;
            
        })
    }
}

interest.init();